{
  "name": "@treino/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@fastify/cors": "^10",
    "@fastify/sensible": "^6",
    "@prisma/client": "^6",
    "@treino/shared": "workspace:*",
    "fastify": "^5"
  },
  "devDependencies": {
    "@treino/tsconfig": "workspace:*",
    "prisma": "^6",
    "tsx": "^4",
    "typescript": "^5.7"
  }
}
